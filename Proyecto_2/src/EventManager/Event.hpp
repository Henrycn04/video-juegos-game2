#ifndef EVENT_HPP
#define EVENT_HPP
/**
 * @brief Base class for all events in the system.
 *
 * This abstract class is intended to be inherited by specific event types.
 * It provides a common interface that can be used to manage and dispatch events.
 */
class Event {
  public:
      /**
     * @brief Default constructor for Event.
     */
   Event() = default;

};
#endif